// If a given string has either x, replace x by y. if the given string has X, replace it by Y.

let stringX = 'X';

let newStringX = stringX.replace('x','X');

console.log(newStringX);

let stringY = 'y';

let newStringY = stringY.replace('x','y');

console.log(newStringY);